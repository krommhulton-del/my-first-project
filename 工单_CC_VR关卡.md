# 工单 · 给 Claude Code（在我的 Windows 电脑、Unity 项目里运行）

## 0. 你的处境
你是 Claude Code，跑在我的 Windows 电脑上：**Unity 2022.3.62f3c1 LTS（个人版）**。你能直接改工程、装包、导入资源、按 Play 测试、看 Console 报错。**我是新手**，请你把活**全部干完、自己测通过、修掉所有报错**，最后用最简单的话告诉我怎么运行、怎么录屏。需要我做选择时（比如选哪个免费角色），一次性给我 2–3 个选项，别卡住。

## 1. 一句话目标
做一个**唯美的、单关卡 VR 小游戏**：《地球 Online · 北京副本 —— 你是她的眼睛》。第一人称 VR，用手柄**牵一个女孩**走过一段记忆，沿途触发旁白与音乐，抓取记忆碎片，做一次抉择，最后「无相」散场。

## 2. 硬性要求（作业评分点，缺一不可）
- 交互 VR 应用，用 **XR Interaction Toolkit**（XR 插件）
- **第一人称漫游** + **VR 手柄交互**
- **UI 两种模式都要有**：Overlay（屏幕）+ WorldSpace（场景里）
- **背景音乐 + 交互音效 + 粒子系统**
- **一个角色**：要有**建模 + 骨骼绑定 + 动画**（用免费的 **Mixamo** 角色即可，自带骨骼+动作）
- **自主玩法**（能玩的机制，不是看片）；老师说**一个关卡**就够

## 3. 技术栈（照此搭）
- **URP** 项目（用它的 Bloom / 调色做唯美效果）
- **XR Interaction Toolkit 2.5.x**（2022.3 对应版）+ XR Plugin Management + **OpenXR**(PC) + **XR Device Simulator**（没头显时也能用键鼠模拟手柄测试/录屏）+ Input System
- **直接导入 XRI 自带的 “Starter Assets” 示例**（Package Manager → XR Interaction Toolkit → Samples），里面有现成的 XR Origin 装备 + 连续移动/转身 + 射线/直接交互器，省掉大量搭建
- 免费资源：
  - 角色 → **Mixamo**（mixamo.com，Adobe 免费）：选一个气质优雅的女性角色，下载 **Idle / Walking / 伸手或牵手** 动画的 FBX（带蒙皮），导入设为 Humanoid
  - 环境 → 一个**免费唯美场景包**（夜景 / 室内 / 抽象空间，挑顺眼的免费 Asset Store 包）
  - 天空盒 → 免费渐变 / 星空天空盒
  - 后处理 → URP **Volume**（Bloom + Vignette + Color Adjustments + Film Grain）

## 4. 玩法 / 关卡（就做这一关，线性、唯美）
1. **载入**：黑场打字机系统文字（见 §6），淡入场景。
2. **遇见女孩**：她站在路口播 Idle。**你伸手/扣扳机靠近她 → 她牵住你的手开始跟随你走**（跟随时切 Walking 动画，停下切 Idle）。
3. **记忆碎片**：沿途几颗发光碎片（XR Grab Interactable）。**手柄抓起 → 放到基座/递给她 → 触发一句旁白 + 音效 + 粒子爆发**。
4. **触发式旁白**：走到节点自动播旁白（**WorldSpace 飘字** 同时 **Overlay 底部字幕**）。
5. **抉择**：**WorldSpace 四选一面板**，手柄射线点选 → 决定结尾前「那个人」那句话。
6. **无相散场**：场景与女孩化作粒子消散 → 黑场浮出落款金句 → 提示重玩（按键/手柄键重开）。
- 评分用「玩法清单」：牵手跟随 / 抓取递物 / 射线点选 / 自由漫游 / 触发叙事 / 分支 / 结算。

## 5. 要写的脚本（XR 装备用 XRI 现成的，下面这些你来写）
- `GirlFollow.cs`：女孩平滑跟随玩家（手或身体），到达即停；用 Animator 切 Idle/Walk；简单避障可选。
- `HandHold.cs`：扳机靠近女孩 → 建立「牵手」（她的手贴合/IK 到你的手），启动跟随。
- `MemoryShard.cs`：XR Grab Interactable，放到目标点触发 UnityEvent（旁白+粒子+音效）。
- `NarrationDirector.cs`：按触发器/距离顺序播旁白（WorldSpace+Overlay），推进到结尾；存分支结果。
- `ChoiceMenu.cs`：WorldSpace 四选一，射线点选，回调分支。
- `DissolveEnding.cs`：结尾粒子消散 + 落款金句 + 重玩。
- `ProceduralAudio.cs`：运行时合成 drone 背景乐 + 交互音效（无需音频文件）；或直接用免费音乐/音效，随你方便。
- 玩家=第一人称，只有**手柄（手）**，不露脸/不做身体（守「男主只露手」的设定）。

> 偷懒提示：我在 GitHub 分支 `claude/ecstatic-darwin-dzbmdq`（仓库 `krommhulton-del/my-first-project`，文件夹 `unity/Assets/EarthOS/`）有一套**可直接复用的 C#**：`ProceduralAudio.cs`（程序化BGM+音效）、`FX.cs`（粒子工厂）、`UIController.cs`（Overlay+WorldSpace UI）、`ChoiceMenu.cs`（四选一）、`ExperienceDirector.cs`（旁白/分支/结尾编排，里面**已含全部 DS 文案常量**）。把它们拿来改，比从零写快很多。**只需把里面用方块/球体的占位几何，换成 Mixamo 角色 + 免费环境即可。**

## 6. 中文文案（DS 定稿，逐字使用，不要自己改写中文）

**载入屏（系统打字机）**
```
> EARTH · OS   v.∞
> 副本编号   #047
> 载入坐标   北京 · 北京
> 宿主等级   LV.18
> 权限开通完毕
> 记忆暂存服务器
> 载入中……
```
**沿路旁白（按顺序触发）**
1. 行李箱的轮子，在北京站前广场，迟疑了一下。
2. 在举目无亲的地方，人，才能成为另一个自己。
3. 凌晨四点，卸妆棉上是粉底、灰尘、和一句没说出口的台词。
4. 在这片钢铁森林里，她把一个愿望，轻轻搁在了车来车往的桥上。
5. 龙舌兰的杯底，映着极静极美的北京。
6. 直到窗外那些流动的灯火，都像他的侧脸，她才知道完了。良辰美景奈何天。

**抉择**：`你，为什么还留在北京？`
- A `梦，还没做完`
- B `已经没有别的地方可去`
- C `等一个人，或者等一个答案`
- D `记不起来了`

**「那个人」回应（按所选）**
- A `这座城市的梦太吵了。`
- B `你看，它也并没有真的留你。`
- C `有些人，真的，只能在北京才会遇见。`
- D `趁酒杯摇曳，快乐还没过期。`

**无相结尾（系统打字机）**
```
> 副本 #047 · 北京 · 北京　已结束
> 记忆封存中……
> 从此刻起，你将遗忘她。
> 被你触碰过的人，也将忘记你。
> 你，带走了什么？
> [ 数据已清除 ]
```
**落款金句（黑场浮出）**
```
你与北京，两不相欠。
恨海情天，终付无言。
```

## 7. 美术 / 气质（唯美 · 古早言情）
- 色板：深邃夜底 `#0D0A0B` / 纸醉酒红 `#7A2A35` / 回忆米象 `#E8DFD3` / 微醺金光 `#B89664` / 冷夜雾蓝 `#2C3A47` / 高光白 `#F5EBD8`
- URP **Bloom** 把发光体/碎片打亮 + 雾 + 暗角 + 轻颗粒 → 电影感
- 我会给几张参考美图（金发美人 / 面纱画风）：用作**海报 / 画框贴图 / loading / 氛围**
- 字体：中文用衬线（思源宋体 Noto Serif SC），系统文字用等宽
- 调性铁律：克制、留白、慢；玩家（你）只有手柄=手，不露脸

## 8. 交付（你做完要给我的）
1. 一个**能跑通**的单关卡 VR 工程：URP + XRI + OpenXR + XR Device Simulator 都配好。
2. Mixamo 角色 + 动画 + 免费环境 + 天空盒 + 后处理 都导入接好。
3. 上面的脚本写好、关卡搭好、UI/音乐/音效/粒子都接上。
4. **你自己进 Play 测试、修掉所有报错**，确保用 **XR Device Simulator 或键鼠**能从头走到尾（牵手→抓碎片→四选一→散场）。
5. 给我**傻瓜级运行步骤 + 录屏方法**（我要录视频做答辩 PPT；没头显就用模拟器/桌面录）。

做完先给我一段「这关怎么玩、点哪个键」的说明。开工吧。
