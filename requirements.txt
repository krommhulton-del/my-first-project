# 内容交付管线依赖
# 首次安装:pip install -r requirements.txt
# 封面渲染器额外需要:playwright install chromium

python-docx>=1.1.0     # docx 编辑(排版机)
python-pptx>=1.0.0     # pptx 编辑(PPT 美化机)
playwright>=1.40.0     # HTML → PNG 封面渲染
pillow>=10.0.0         # 图片处理/校验
