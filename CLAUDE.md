# 东玄卜卦 · 项目宪法

这份文件是项目的**唯一权威约定**。任何一次会话开工前先读它;它与你的记忆冲突时,**以它为准**。
改动了架构、口径、铁律或流程,**必须同步改这份文件**——文件与代码不符,视同 bug。

---

## 一、这是什么

一个纯静态的中式术数网页程序,零后端。用户在浏览器里起卦、排八字、看运势、择日;
需要深度解读时,由用户自己的 API Key 直连大模型。
产物有两个:仓库里的多文件版(可挂网页),与 `dist/dongxuan.html` 单文件桌面版(双击即用,无外部依赖)。

**角色设定「东玄」**:街坊里那个断得准的老先生。说人话、先给答案再讲道理、凶就报凶、不安慰不劝解。

---

## 二、铁律(不可协商,违反即废稿)

1. **零说教**。不讲道理、不劝人、不发感慨、不灌鸡汤、不写「你要明白」「学会××」「与其…不如…」这类句子。只留:事、断、做法。
2. **先答案后凭据**。每一段第一句就是结论——成不成、几成、哪年哪月哪日、多少钱、几厘米。铺垫句开头一律算废稿。
3. **禁空话**。「机遇与挑战并存/顺其自然/保持平常心/静观其变/仅供参考/因人而异/总的来说」等一律禁用。把握度用「几成」说死,做法具体到动作与时间。
4. **一枝一卦**。追问超出已起诸卦范围的,明说要再起一卦,不许拿旧卦硬答新问题。
5. **不推荐花钱消灾**。开光、请购法物、付费化解——一概不推荐,任何情况下不松口。传统做法(避其方、择其时、穿用其色、清理居处、静养、行善)可以给。
6. **依卦有据,不无中生有**。卦面/命盘没照到的地方,明写「此卦未及此节」。
7. **不比卦面乐观或悲观半分**。结论的吉凶与把握,必须与程序算出的力量分挂钩。
8. **术语不上稿**。用神、世应、旬空、十神、喜忌、干支、旺相休囚这些只在推演时用,写给客人看的成稿里一个都不许出现。
9. **不鸡贼**。起卦随机源必须是 `crypto.getRandomValues`;绝不为了迎合预设答案而做手脚。
10. **隐私**:API Key 与用户生辰**只存本机 localStorage**,绝不写进代码、绝不上传、绝不进仓库。

---

## 三、准确率的三层诚实分级(对外说话的底线)

对外只许按这个分级表态,**不许把下层的把握说成上层的**:

| 层 | 内容 | 现状 | 能说什么 |
|---|---|---|---|
| **排盘层** | 历法、干支、纳甲、八宫、神煞位置、方位角 | **有客观标准,已逐项对照,零误差** | 可以说「准」 |
| **断法层** | 旺衰、用神、格局、吉凶方向 | 与**通行简法**一致,流派间本就有分歧 | 只能说「与通行口径一致」,并把计分摊开给人看 |
| **事件层** | 「2028 年有姻缘」这类具体应验 | **零外部回测,无任何命中率数据** | **什么都不许承诺**。只说「窗口」,不说「一定」 |

**测试的两类,必须分清**:
- **外部对照测试**(约 60 项):京房八宫全表、纳甲干支、六神起例、纳音、空亡、节气历法、名人四柱、三钱概率、球面方位——**证明准确**。
- **内部一致性测试**(约 220 项):单调性、口径统一、方向自洽、禁词、结构——**只证明程序不犯浑,不证明断得准**。

写测试时想清楚自己在写哪一类,并在测试文件抬头注明。

**统计类测试要防假阳性**:卡方/分布这类检验按 0.05 显著性写,自己就会有 5% 概率随机变红(已发生过一次)。
一律用 **0.001 显著性**,并加一条**下界**——分布「过于完美」反而说明随机源被动过手脚。

---

## 四、架构:每个口径只许有一个出处

**绝对规则:同一件事只能有一处算。** 已经因为违反它出过两次事故(择日与运势对同一天给出相反结论;`PKEY` 常量重名导致整页白屏),现在有结构测试守着。

| 模块 | 独家负责 | 别处不许重复实现 |
|---|---|---|
| `gua-core.js` | 三钱起卦、卦变、经文取材 | 随机源 |
| `gua-data.js` | 六十四卦数据 | |
| `najia.js` | 干支历、节气、京房八宫、纳甲、六亲、六神、旬空、**伏神、爻的旺衰、进退神、用神取法** | 任何断卦元素 |
| `lunar.js` | 农历 | |
| `bazi.js` | 四柱排盘、真太阳时、**旺衰(双向称量)、喜用神、从格、大运、神煞、人元司令** | **`judgeStrength` / `pickYongShen` 只此一份** |
| `yunshi.js` | 流年月日判读、具体宜忌事项 | 喜忌一律取 `chart.yong` |
| `jiri.js` | 黄历择吉(建除、值神、星宿、彭祖、消息卦)、两层分账 | 「对你」层必须吃 `chart.yong` |
| `dashi.js` | 人生大事年表、流月、年龄闸门 | |
| `wenji.js` | 问机:年/月/日三层应期、画像、贵人 | 只做串联,不自己另算喜忌 |
| `dili.js` | 地名坐标、方位角、旺地判断 | |
| `sanmei.js` | 算命学人体星図 | |
| `meihua/xiaoliuren/qimen/fenke` | 各自法门与分科 | |

**前端**:`index.html`(主程序,四视图路由 + 各专项板块)、`yunshi.html`(运势独立页)。
**命盘唯一入口**:`index.html` 里只许有一处调用 `Bazi.chart`,即 `dxBirthChart()`;所有板块走它。有结构测试守着。

**id 前缀已被占用**(新板块别再撞):`wj-`=未来镜、`wq-`=问机、`jr-`=吉日、`xy-`=心愿、`yl-`=姻缘、`dl-`=地利、`zy-`=转运、`dw-`=大问。

**localStorage 键**:`dongxuan_birth` / `_birth_hour` / `_birth_place` / `_gender` / `_age` / `_theme` / `_api_key` / `_ds_key` / `_history_v1` / `_profiles_v1` / `_profile_cur` / `_ai_provider`。

---

## 五、核心设计哲学:程序算死,AI 只解释

结论必须由**程序**按古法算出来并写进材料,大模型只负责翻成人话。这样才能保证同一副盘每次结论一致、且可追溯、且不会被模型的乐观倾向带跑。

已经这么做的:旺衰计分、喜用神、伏神与飞伏生克、爻的力量分、年表事型证据链、应期三层、答案之锚(取向/取期/取数)、未来镜的基调锁定。

**新写任何断法,先问一句:这个结论是程序算的,还是模型编的?** 后者一律不收。

---

## 六、发版流程(一步都不许省)

1. 改版本号**三处**:`index.html` 的 seal 与 footer、`yunshi.html` 的 seal、`sw.js` 的 `CACHE`
2. `node build-single.mjs`(会把 yunshi 整页内联进单文件,并注入浮层)
3. `npm test`(17 套)+ `npm run e2e`(主 e2e / 运势 e2e / 单文件 e2e)——**全绿才继续**
4. `git commit`,信息写清「发现了什么问题、怎么改的、验了什么」,结尾带:
   ```
   Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
   Claude-Session: <本次会话链接>
   ```
5. `git push -q -u origin claude/divination-chinese-books-6nk58q`(**只推这个分支**)
6. `SendUserFile dist/dongxuan.html`

**Service Worker 用 network-first**(曾因 stale-while-revalidate 让用户一直看到旧版,以为改动没生效)。每次发版必须动 `CACHE` 版本号。

---

## 七、回测语料格式(填了才用得上)

事件层要出命中率,就得有真实案例。**格式定死如下**,存 `data/backtest-cases.json`:

```json
[
  {
    "id": "case-001",
    "name": "匿名或代号",
    "gender": "男",
    "birth": "1978-03-09",
    "hour": "14:00",
    "hourKnown": true,
    "place": "北京",
    "source": "本人自述 / 公开报道链接",
    "events": [
      { "year": 2011, "month": 5, "type": "yinyuan", "what": "结婚", "confidence": "确凿" },
      { "year": 2016, "type": "shiye",  "what": "升任部门负责人", "confidence": "确凿" },
      { "year": 2020, "type": "caiyun", "what": "投资亏损约百万", "confidence": "大致" }
    ]
  }
]
```

- `type` 取值必须是 `dashi.js` 的事型 key:`yinyuan / shiye / caiyun / wenshu / biandong / zinv / jiankang / guanfei`
- `month` 可省;`hourKnown: false` 时时柱不可信,该例只用于年层回测
- `confidence`:`确凿 / 大致 / 存疑`;`存疑` 的不计入命中率,只作参考

**公布规矩**:命中率必须连同**样本量、口径(命中指窗口覆盖该年还是该月)、落空的例子**一起公布。只报命中不报落空,等同作假。

---

## 八、已知边界(不许假装没有)

- **古籍原文抓不到**:本环境网络策略拦截外网(实测 wikisource/ctext/gutenberg 全 403)。因此代码里凡涉出处,一律写「**通行口径,出处待核**」,不许写「依《某书》某章」。拿到原文后再逐条挂出处。
- **节气时刻误差 1.5–6 分钟**(对公开历书实测)。只影响交节前后几分钟出生者的月柱,约万分之一。
- **命宫/身宫未做**:各派算法不一,手上没有可核出处,宁缺勿错。
- **1949 年前中国五时区未处理**:民国时期出生者的经度校正按现行东八区算,可能有偏差。
- **知识缺口(按优先级)**:穷通宝鉴逐月调候全表(现仅粗糙的冬火夏水)· 格局法八格的成败救应(现仅扶抑法)· 神煞全表(现仅十余个)· 六爻应期细法(值日/合日/冲空/填实/墓库只做一半)· 盲派体系未碰。

---

## 九、待办队列

1. ~~起卦断卦引擎体检~~ 已完成(v0.58)
2. ~~问机板块~~ 已完成(v0.59)
3. **基建**:本文件 → 三个 SOP skill → 断语体检员 agent
4. **地利板块重做**:精确到时辰(与八字同口径)、更专业、结合现代居住实际
5. 名人/真人回测语料入库,公布命中率
6. 出处补挂(等古籍可得)

---

## 十、工作方式

- **改断法之前先跑数据找结构性错误**,别靠直觉调参。已用这个办法揪出:从格误判率 58%、择日与运势口径分家、伏神完全缺失、夏令时挂在出生地上。
- **每修一个洞,钉一条测试**,并在测试文件抬头写清「缘起」——为什么有这条测试。
- **自己的测试抓到自己的错,就照实说**。这是这个项目最值钱的习惯。
- 用户报 bug 时,**先用数据证实或证伪**,再动手;不要辩解,也不要不加验证就认。
